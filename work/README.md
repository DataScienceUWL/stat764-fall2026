# Your workspace

Copy notebooks from `course/` into this folder before you edit them.

Everything here except this README is ignored by git, so it stays on your machine and
never conflicts with `git pull`.
